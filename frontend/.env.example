# ============================================
# 복권 DApp 환경변수 설정
# ============================================
# 이 파일을 .env.local로 복사한 후 값을 입력하세요.
# cp .env.example .env.local

# 스마트 컨트랙트 주소 (Remix에서 배포 후 입력)
VITE_CONTRACT_ADDRESS=0x_your_deployed_contract_address

# Sepolia 체인 ID (변경 불필요)
VITE_CHAIN_ID=11155111

# Infura API 키 (선택사항 - 네트워크 추가 시 사용)
# https://infura.io 에서 무료 계정 생성 후 API 키 발급
VITE_INFURA_API_KEY=your_infura_api_key
